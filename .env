# Rename this file to .env or copy values into your environment before running docker-compose
# MongoDB connection string (Atlas/URI). Example:
# MONGO_URL="mongodb+srv://<user>:<pass>@cluster0.mongodb.net/mydb?retryWrites=true&w=majority"
MONGO_URL=

# Gemini API key (set your key here)
GEMINI_API_KEY=

# CORS allowed origins (comma-separated). Keep this for local dev:
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:3000

# Frontend API URL (used when building the frontend). When using docker-compose,
# the frontend is configured to call http://backend:8000 by default.
REACT_APP_API_URL=http://backend:8000
